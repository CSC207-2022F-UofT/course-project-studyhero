package Timer.TimerUI;

import javax.swing.*;

public class StartTimerButton extends Button {
    static JButton startTimerButton = createButton("Start Timer");
    public static JButton startTimerButton() {
        return startTimerButton;
    }
}
